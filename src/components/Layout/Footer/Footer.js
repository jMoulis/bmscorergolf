import styled from '@emotion/styled';

const Footer = styled.footer`
  height: 10rem;
  background-color: ${({ theme }) => theme.colors.darkGray.hex};
`;

Footer.propTypes = {};

export default Footer;
